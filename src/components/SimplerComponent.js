import React from 'react';

const SimplerCompnonent = props => {
    return(
        <div onClick={props.handleClick}>
            I am just happy
        </div>
    )
}

export default SimplerCompnonent